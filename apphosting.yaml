# Firebase App Hosting configuration for Astro SSR.
# PORT is injected by App Hosting and must not be hard-coded here.
scripts:
  buildCommand: npm run build
  runCommand: npm run start

runConfig:
  maxInstances: 10
  concurrency: 80
  cpu: 1
  memoryMiB: 512
